(function( $ ){

	if (!$.SDQ) {
		$.SDQ = {};	
	};

	// Estas cedulas fueron emitidas por la JCE y no cumplen con el digito verificador
	var cedulasLocas = ['00000000018','11111111123']; // El listado es bien largo, falta agregar las demas...

})( jQuery );